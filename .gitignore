# Slurm output and error logs; large, temporary, and not needed in git
logs/
# Raw or intermediate data; large
data/
# Output files from mapping; generated files, can be recreated
results/
# Reference genome FASTA; large file, can be downloaded separately
resources/